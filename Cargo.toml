[package]
name = "rustgram"
version = "0.1.0"
edition = "2021"
description = "A modern, high performance, and fully asynchronous Telegram MTProto framework in Rust for both user accounts and bots."
license = "MIT"

[lib]
name = "rustgram"
path = "rustgram/lib.rs"

[dependencies]
